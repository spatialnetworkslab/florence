// import RedrawManager from '../../src/components/Core/Graphic/RedrawManager.js'

describe('RedrawManager: no overlapping sections, no caching', () => {
  it('correctly rerenders (1 layer, 1 section)', () => {

  })
})
