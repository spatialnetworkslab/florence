import { getGeometry, ensureValidCombination } from './generateCoordinates.js'
import generateArrayOfLength from '../utils/generateArrayOfLength.js'
import getIndexArray from '../utils/getIndexArray.js'

export default function (geometryProps, sectionContext, coordinateTransformationContext, interpolate, indexProp) {
  ensureValidCombination(geometryProps)
  

  return {}
}
