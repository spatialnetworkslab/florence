export function isValid(x, y) {
    // continue
}

export function createPosYCoords (vjust, yRange, orient, height, offset, titleFontSize) {
    // continue
}


export function createPosXCoords (vjust, yRange, orient, height, offset, titleFontSize) {
    // continue
}