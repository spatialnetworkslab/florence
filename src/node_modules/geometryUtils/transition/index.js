import { eitherIsAPoint, createPointTransition } from './pointTransition.js'

export function transitionGeometries (fromLayer, toLayer) {
  // TODO
}

export function transitionGeometry (fromGeometry, toGeometry) {
  // Points are a special case
  if (eitherIsAPoint(fromGeometry, toGeometry)) {
    return createPointTransition(fromGeometry, toGeometry)
  }

  if 
}
